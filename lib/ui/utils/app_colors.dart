import 'package:flutter/material.dart';

abstract final class AppColors {
  static const Color lightBlue = Color(0xffd62525);
  static const Color blue = Color(0xff6992ea);
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}

/// const -> Fully immutable and compile-time constant
/// final -> half immutable (runtime constant)